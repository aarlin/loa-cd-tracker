import { CooldownDirective } from './cooldown.directive';

describe('CooldownDirective', () => {
  it('should create an instance', () => {
    const directive = new CooldownDirective();
    expect(directive).toBeTruthy();
  });
});
